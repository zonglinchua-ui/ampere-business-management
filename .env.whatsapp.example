# WhatsApp Notifications Configuration (WAHA)
# ================================================

# Enable/Disable WhatsApp notifications
WAHA_ENABLED=true

# WAHA API URL (default: http://localhost:3000)
# If running WAHA on the same server, use localhost
# If running on a different server, use the server's IP or domain
WAHA_API_URL=http://localhost:3000

# WAHA API Key (generated during WAHA initialization)
# Get this from the .env file created by: docker run --rm -v "$(pwd)":/app/env devlikeapro/waha init-waha /app/env
WAHA_API_KEY=your_waha_api_key_here

# WAHA Session Name (default: default)
# This is the session name you configured in WAHA dashboard
WAHA_SESSION=default

# Cron Secret (optional, for securing cron endpoints)
# Generate a random UUID or long string
CRON_SECRET=your_random_secret_here
